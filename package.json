{
  "name": "prototype-railway",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true,
  "scripts": {
    "start": "cd backend && python -m gunicorn app:app --bind 0.0.0.0:$PORT",
    "build": "cd backend && pip install -r requirements.txt"
  }
} 