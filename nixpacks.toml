[phases.setup]
nixPkgs = ["python39", "gcc", "binutils", "gdal", "libstdc++"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["cd backend && echo 'Build phase complete'"]

[start]
cmd = "cd backend && python -m gunicorn app:app --bind 0.0.0.0:$PORT" 